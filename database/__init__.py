"""Top-level database package for Lambda fallback imports.

This enables `from database.loan_service import ...` to work in environments
where only the top-level `database/` directory is present.
"""

